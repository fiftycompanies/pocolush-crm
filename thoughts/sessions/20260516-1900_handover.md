# 인계인수서 — 2026-05-16 19:00 → 다음 세션

## 🎯 현재 prod 상태

**라이브 운영 정상**. 농장 현황 페이지 신설 + 사이드바 [일별 운영] 순서 변경 + 평상/농장 현황 "관리하기 →" 버튼 추가 완료. 평상 이력 검색 + 13 RPC 통일 + 자동갱신 UX 모두 prod 적용. ⚠ **Realtime 401 (28건) 여전히 잔존** — 옵션 2 (localStorage 동기 읽기) 다음 세션 우선.

---

## 📦 이번 세션 누적 변경 (10 커밋 + 4 마이그)

```
b0701b9  StatsCards nested Link 풀기
38630f0  /dashboard #418 useState/useEffect
9de7b51  supabase client singleton + accessToken
b3b5c3d  평상 이력 검색 (마이그 082 + 3 컴포넌트)
40e2669  082b hotfix (created_at ambiguity)
7bc7729  PR 1 자동갱신 UX (animate-spin)
676aaef  PR 2 Realtime 401 옵션1 setAuth 즉시 (효과 미발현)
2d6dce8  PR 3 13 RPC is_admin 통일 마이그 083 (10건)
6edb9a8  농장 현황 페이지 + 사이드바 + 관리하기 (Q4=30일)
2eeb431  farms-board loading state hotfix
a0b0c2e  마이그 084 farm_zones_active view 갱신
```

### DB 마이그 (prod 적용)
- **082** search_bbq_reservations RPC + 인덱스 3종 + pg_trgm
- **082b** created_at ambiguity hotfix
- **083** 10 RPC admin 체크 통일 (assert_admin_with_audit)
- **084** farm_zones_active view (is_operational 포함)

---

## 🛡 보호 장치 (이번 세션 신규)

| 종류 | 항목 |
|---|---|
| trigram 인덱스 | members.name/phone 부분 일치 |
| 1h dedup PIPA audit | bbq_history_search action |
| assert_admin_with_audit | 10 RPC unauthorized audit_logs 보장 |
| singleton supabase client | Realtime connection 공유 (효과 미발현) |
| view 자동 컬럼 포함 | farm_zones_active SELECT * |
| loading state 명시 | farms-board 첫 fetch race 회피 |

---

## 📊 prod 데이터 (19:00)

```
회원              60+ approved
농장              60 (A존 40 + B존 20 운영) / 활성 34 / 만료 30일 0
BBQ 예약          30 / 평상 1상품 / 이벤트 1
공지              7
audit_logs        250+ row (079 dedup 정상)
```

---

## 🟡 잔여 백로그 (다음 세션)

| 우선 | 항목 | 추정 | 상태 |
|---|---|---|---|
| **🔴 P0** | **Realtime 401 옵션 2** (localStorage 동기 읽기) | 1h | **3차 시도 후 미해결** |
| 🟡 P1 | U2 모바일 햄버거 활성화 (5/22 burn-in 후) | 5m | env add + redeploy |
| 🟡 P1 | Phase D E2E spec 4건 (H1/H4/H6/H7) | 0.5일 | 백로그 |
| 🟢 P2 | 농장 현황 §이력 검색 섹션 (BBQ 패턴 답습) | 4h | Phase 2 plan §10 |
| 🟢 P2 | get_farms_board RPC 분리 (useFarms → RPC) | 2h | Phase 2 plan §10 |
| 🟢 P2 | 만료 임박 7일/30일 단계 분리 (KPI 2개) | 1h | Phase 2 |
| 🟢 P3 | U8 ConfirmDialog | 0.5일 | 백로그 |
| 🟢 P3 | G8 Playwright 1.59 → 1.60 | 0.25일 | 백로그 |
| 🟢 P3 | G9 CI 야간 자동 E2E | 0.25일 | 백로그 |
| 🟢 P3 | 알림톡 D-30 cron | 0.5일 | 백로그 |

---

## ⚠️ Realtime 401 잔존 — 우선 후속

### 3차 시도 후에도 효과 미발현
1. TopBar setAuth (5361889) — 8건 잔존
2. lib/supabase/client.ts singleton + accessToken (9de7b51) — 16건 잔존
3. createBrowserClient 직후 명시적 setAuth IIFE (676aaef) — **28건 잔존**

### 다음 시도 옵션 (plan §3 우선순위)
- **옵션 2** ⭐ — localStorage 동기 읽기:
  ```ts
  const supabaseKey = `sb-${projectRef}-auth-token`;
  const stored = typeof window !== 'undefined' ? localStorage.getItem(supabaseKey) : null;
  const initialToken = stored ? JSON.parse(stored).access_token : null;
  // initialToken 을 createBrowserClient 옵션의 global headers 또는 다른 경로로 전달
  ```
- 옵션 3 — @supabase/supabase-js createClient 직접 호출 (SSR cookie 분리 처리)
- 옵션 4 — supabase-js debug 모드 활성화 + 패키지 issue 확인 / Github issue 검색

### 영향 평가
- 운영 영향 0 (콘솔 노이즈만)
- audit_logs 영향 0 (079 dedup)
- 회원 측 회귀 0 (/m/login 200)
- 롤백 권고 없음

---

## 🟢 사이드바 V2 최종 구조 (활성 중)

```
[일별 운영]
  대시보드
  농장 현황       ← NEW (Grid3x3)
  평상 현황       ← 라벨 단축 (예약 제거)
  신청 관리
  문의 관리

[회원]            회원 관리 / 회원권 관리 / 공지 관리
[자원·시설]       농장 관리 / 임대 계약 / 평상 설정
[상거래]          스토어 설정 / 플랜 관리 / 쿠폰 설정
[콘텐츠]          블로그 관리
[시스템 하단]     경고(admin) / 감사 로그 / 알림 설정 / 설정
```

---

## 🔴 운영 정합성 체크 SQL (다음 세션 진입 전)

```sql
SELECT
  (SELECT COUNT(*) FROM bbq_reservations) AS bbq_total,
  (SELECT COUNT(*) FROM farms WHERE deleted_at IS NULL) AS farms_total,
  (SELECT COUNT(*) FROM memberships WHERE status='active') AS memberships_active,
  (SELECT COUNT(*) FROM memberships WHERE status='active' AND end_date <= CURRENT_DATE + INTERVAL '30 days') AS expiring_30d,
  (SELECT COUNT(*) FROM audit_logs WHERE action='bbq_board_read' AND created_at > NOW() - INTERVAL '24 hours') AS dedup_check,
  (SELECT COUNT(*) FROM audit_logs WHERE action='bbq_history_search' AND created_at > NOW() - INTERVAL '24 hours') AS hist_dedup,
  (SELECT COUNT(*) FROM audit_logs WHERE action LIKE '%_unauthorized') AS unauthorized_total,
  (SELECT COUNT(*) FROM bbq_facilities WHERE name ILIKE 'QATEST_%') AS test_residue;
```

기대:
- bbq_total 30+
- farms_total 60
- memberships_active 34+
- expiring_30d (현재 0)
- dedup_check < 30 / hist_dedup < 24
- unauthorized_total = 083 마이그 적용 후 admin 외 호출 시 누적
- test_residue 0

---

## 🧠 컨텍스트 복구 파일

1. 이 인계: `thoughts/sessions/20260516-1900_handover.md`
2. 세션 기록: `thoughts/sessions/20260516-1900_session_record.md`
3. Realtime 401 plan: `thoughts/plans/20260516-1530_residual_backlog_4items_plan.md` §3 (옵션 2)
4. 농장 현황 plan (구현 완료): `thoughts/plans/20260516-1730_farms_board_and_management_pivot_plan.md`
5. 이전 세션: `thoughts/sessions/20260516-1230_handover.md`

---

## 핵심 상수 / 링크

- admin 계정: `admin@pocolush.co.kr` / `123456`
- Supabase project: `lhuaxmzsvrmjavanunnv`
- prod URL (어드민): https://app.pocolush.com
- prod URL (공개): https://www.pocolush.com
- 다음 마이그 번호: **085**
- Vercel CLI: 50.42.0 → **54.1.0 권고**
- Vercel env: `NEXT_PUBLIC_SIDEBAR_IA_V2=1` 활성 / `NEXT_PUBLIC_SIDEBAR_MOBILE_V2` 미설정 (5/22 예약)

---

## END — 농장 현황 신설 + 사이드바 + 관리하기 + 13 RPC + 평상 이력 모두 정상. Realtime 401 잔존 (다음 세션 옵션 2).
