# 인계인수서 — 2026-05-16 23:30 → 다음 세션

## 🎯 현재 prod 상태

**라이브 운영 정상**. 농장 §이력 검색 (마이그 087) + TopBar realtime publication fix (마이그 086) prod 배포 완료. 6커밋 + 3 마이그 (085/086/087) 모두 적용. Cloudflare wildcard SSL 만료 알림은 운영 영향 0 (Vercel Let's Encrypt 자동 갱신 중).

---

## 📦 이번 세션 누적 변경 (총 7 커밋 + 3 마이그)

```
97d4a72  fix(realtime): 옵션 5 — localStorage + global.headers + realtime.params (3건)
ea11c10  feat(085): get_farms_board RPC + useFarmsBoard ✅ (4→1 round)
5cde1b3  fix(realtime): 옵션 6 — cookie 동기 파싱 (6건, apikey 충돌 발견)
01c7f03  fix(realtime): 옵션 7 — realtime.params 표준화 + IIFE setAuth (5건, 채택) ✅
a6af5eb  fix(ci): TruffleHog Secret Scan 이벤트별 base/head 분기 ✅
ecf418a  docs(session): 22:00 인계
4ab76a6  feat(086+087): TopBar realtime publication + 농장 §이력 검색 섹션 ✅ (검증 미완)
```

### DB 마이그 (prod 적용)
- **085** `get_farms_board()` RPC (4 round → 1)
- **086** notifications 테이블 → supabase_realtime publication 추가
- **087** `search_farm_rentals_history()` RPC + 인덱스 4종 (customers trgm + farm_rentals 인덱스 2종)

---

## ✅ PR 4 (마이그 086 + 087) — 검증 미완 / 코드 정상

### 마이그 086: TopBar 알림 publication 추가
- `ALTER PUBLICATION supabase_realtime ADD TABLE public.notifications` (idempotent)
- TopBar.tsx 가 `notifications` 채널 구독했으나 publication 미포함이었음
- 효과: 어드민 알림 실시간 toast 활성화 (Phase 0 의도 복원)
- 영향: 회원 측 0 (member_notifications 별개)

### 마이그 087: 농장 §이력 검색 RPC
- 신규 RPC `search_farm_rentals_history(p_date_from, p_date_to, p_query, p_status[], p_plan[], p_zone_id, p_page, p_limit)`
- 인덱스: `customers(name)` trgm, `customers(phone)` trgm, `farm_rentals(end_date,status)`, `farm_rentals(start_date)`
- 기간 필터: `start_date <= dateTo AND end_date >= dateFrom` (overlap — active/expired 모두 포함)
- admin only + assert_admin_with_audit + PIPA 1h dedup
- 동작 DB 수치: rentals 80건 (active 34 / expired 33 / cancelled 11), customers 144건

### UI 추가 (BBQ HistorySection 패턴 답습)
- `lib/use-farm-history.ts` (FarmHistoryFilters + FarmHistoryRow + useFarmHistory)
- `components/admin-farms/FarmHistoryFilterBar.tsx` (기간 preset 30/90/365일 + 검색 + 존 select + 상태/플랜 chip)
- `components/admin-farms/FarmHistoryList.tsx` (표 + 페이지네이션 + 행 클릭)
- `components/admin-farms/FarmHistorySection.tsx`
- `app/dashboard/farms-board/page.tsx` 하단 추가 + 행 클릭 시 FarmDrawer 재활용

### 검증 (다음 세션 우선)
- tsc 0 / build 0 ✅
- 마이그 prod 적용 완료 ✅
- **Playwright 검증 미완** — 다음 세션 진입 시 수행:
  - §이력 섹션 렌더링 (h3 "임대 이력 검색")
  - 매트릭스 60셀 회귀 0 (FarmsBoardMatrix)
  - search_farm_rentals_history RPC 호출 + 결과 표시
  - 검색어/상태/플랜 필터 → RPC 재호출
  - TopBar 알림 실시간 toast (notifications INSERT 시)

---

## ⚠️ Realtime 401 — 6차 시도 (옵션 7) 유지

| 시도 | socketerror |
|---|---|
| 1차 (TopBar setAuth) | 8건 |
| 2차 (singleton+accessToken) | 16건 |
| 3차 (IIFE setAuth) | 28건 (역효과) |
| 4차 옵션 5 (localStorage) | 3건 |
| 5차 옵션 6 (cookie 동기) | 6건 |
| **6차 옵션 7 (표준 흐름) ✅** | **5건** |

진단: WebSocket upgrade 자체 401 (sent/received frames 0). server-side 원인.

다음 세션 옵션:
- (C) Supabase Dashboard Realtime Authorization 점검
- (D) Supabase support / GitHub issue 등록

---

## 🌐 Cloudflare 이메일 — 운영 영향 0 (무시 가능)

사용자 수신: "Validate the domain pocolush.com to renew its SSL certificate" (Cloudflare wildcard cert 갱신 알림, 2026-06-16 만료)

### 진단
| 도메인 | 인프라 | SSL 발급자 | 만료 |
|---|---|---|---|
| `app.pocolush.com` | Vercel | Let's Encrypt R12 | 2026-07-09 |
| `www.pocolush.com` | Vercel | Let's Encrypt R12 | 2026-07-09 |
| `pocolush.com` | Vercel | Let's Encrypt R13 | 2026-07-09 |
| Cloudflare wildcard `*.pocolush.com` | **미사용** | (이메일 대상) | 2026-06-16 |

- DNS 만 Cloudflare 호스팅 (NS: sonia/gabriel.ns.cloudflare.com)
- A 레코드 모두 Vercel IP (216.150.1.65, 216.150.16.65) — Cloudflare proxy OFF
- 실제 SSL termination = **Vercel Let's Encrypt 자동 갱신** (90일)

### 권고
1. 이메일 무시 OK
2. (선택) Cloudflare Dashboard → SSL/TLS → Edge Certificates 에서 미사용 wildcard cert 삭제하면 알림 중단
3. Vercel 자동 갱신은 7/9 만료 30일 전 자동 처리됨

---

## 🟡 잔여 백로그 (다음 세션)

| 우선 | 항목 | 추정 | 상태 |
|---|---|---|---|
| 🔴 **즉시** | **4ab76a6 Playwright 검증 (§이력 + TopBar 실시간 toast)** | 15m | **prod 배포 완료, 검증만 남음** |
| 🟡 P1 | U2 모바일 햄버거 활성화 (5/22 burn-in 후) | 5m | env add + redeploy |
| 🔵 P2 | Realtime 401 (C) Supabase Dashboard Realtime Authorization | 1h | server-side risk MID |
| 🔵 P2 | Realtime 401 (D) Supabase support / GitHub issue | 30m | 0 risk |
| 🟢 P2 | 만료 임박 7일/30일 단계 분리 (KPI 2개) | 1h | Phase 2 |
| 🟢 P3 | Cloudflare wildcard cert 정리 (Dashboard) | 5m | 알림 중단 |
| 🟢 P3 | U8 ConfirmDialog | 0.5일 | 백로그 |
| 🟢 P3 | G8 Playwright 1.59 → 1.60 | 0.25일 | 백로그 |
| 🟢 P3 | 알림톡 D-30 cron | 0.5일 | 백로그 |

---

## 📊 prod 데이터 (23:30)

```
회원              60+ approved
농장              60 / 활성 34 / 만료 33 / 취소 11 (전체 80 계약)
임차인 (customers) 144
BBQ 예약          30+ / 평상 1상품 / 이벤트 1
공지              7
인덱스 (087 신규)  customers name/phone trgm + farm_rentals end_date/start_date
realtime publication  notifications, member_notifications, bbq_reservations, notices
```

---

## 🛡 이번 세션 신규 보호 장치

| 종류 | 항목 |
|---|---|
| RPC admin only | get_farms_board (085), search_farm_rentals_history (087) |
| PIPA 1h dedup | farms_board_view, farm_history_search action |
| Trigram 인덱스 | customers name/phone (087), members name/phone (082 누적) |
| Realtime publication 정합성 | notifications (086 추가) |
| CI workflow 안정화 | TruffleHog 이벤트별 base/head 분기 (a6af5eb) |
| realtime-js 표준 흐름 | params apikey only + IIFE setAuth (옵션 7) |

---

## 🔴 운영 정합성 체크 SQL (다음 세션 진입 전)

```sql
SELECT
  (SELECT COUNT(*) FROM bbq_reservations) AS bbq_total,
  (SELECT COUNT(*) FROM farms WHERE deleted_at IS NULL) AS farms_total,
  (SELECT COUNT(*) FROM farm_rentals) AS rentals_total,
  (SELECT COUNT(*) FROM farm_rentals WHERE status='active') AS rentals_active,
  (SELECT COUNT(*) FROM memberships WHERE status='active') AS memberships_active,
  (SELECT COUNT(*) FROM audit_logs WHERE action='farm_history_search' AND created_at > NOW() - INTERVAL '24 hours') AS farm_hist_dedup,
  (SELECT COUNT(*) FROM audit_logs WHERE action='farms_board_view' AND created_at > NOW() - INTERVAL '24 hours') AS farms_board_dedup,
  (SELECT COUNT(*) FROM audit_logs WHERE action LIKE '%_unauthorized') AS unauthorized_total,
  (SELECT EXISTS(SELECT 1 FROM pg_publication_tables WHERE pubname='supabase_realtime' AND tablename='notifications')) AS notif_pub_ok,
  (SELECT proname FROM pg_proc WHERE proname='search_farm_rentals_history' LIMIT 1) AS rpc_087_exists;
```

기대:
- farms_total 60 / rentals_active 34 / rentals_total 80
- notif_pub_ok true (086 적용 확인)
- rpc_087_exists 'search_farm_rentals_history' (087 적용 확인)
- *_dedup < 24 / unauthorized 0

---

## 🧠 컨텍스트 복구 파일

1. 이 인계: `thoughts/sessions/20260516-2330_handover.md`
2. 22:00 인계: `thoughts/sessions/20260516-2200_handover.md`
3. 19:00 인계: `thoughts/sessions/20260516-1900_handover.md`
4. plan: `thoughts/plans/20260516-2000_realtime_401_and_farms_rpc_plan.md`
5. research: `thoughts/research/20260516-1930_realtime_401_deep_and_farms_rpc_research.md`

---

## 핵심 상수 / 링크

- admin: `admin@pocolush.co.kr` / `123456`
- Supabase project: `lhuaxmzsvrmjavanunnv`
- prod URL: `https://app.pocolush.com` / `https://www.pocolush.com`
- 다음 마이그 번호: **088**
- Vercel CLI: 50.42.0 → 54.1.0 권고
- DNS: Cloudflare NS 위임 + Vercel A 레코드 (proxy OFF) + Vercel Let's Encrypt SSL

---

## END — 농장 §이력 검색 + TopBar realtime fix prod 배포 완료. Playwright 검증만 다음 세션 우선.
