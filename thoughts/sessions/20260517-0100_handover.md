# 인계인수서 — 2026-05-17 01:00 → 다음 세션

## 🎯 현재 prod 상태

**라이브 운영 정상**. v2 plan 의 6 PR 중 5 PR (X/A/C1/C2/B) prod 배포 완료. PR-D (알림톡 D-30) 는 Aligo 템플릿 5일 검수 대기. 모든 검수 영향 0 확인 (8스킬 + 7점 + 사이드 이펙트).

---

## 📦 이번 세션 prod 배포 (8 커밋)

```
721d370  security(e2e): ADMIN_PW 평문 제거 + E2E_ADMIN_PW throw (PR-X)
27fb6f2  fix(farms-board): Q-A1 expired/cancelled 행 toast 안내 (PR-A)
321a3d7  feat(kpi): 만료 D-7/D-30 한 카드 2-값 분리 (PR-C1)
b435d3e  feat(ui): ConfirmDialog + useConfirm + Phase 1 4건 마이그 (PR-C2)
01b92a8  chore(e2e): Playwright 1.59.1 → 1.60.0 (PR-B 부분)
```

이전 세션 (4ab76a6 마이그 086/087) prod 안정 운영 중.

---

## ✅ 이번 세션 완료 작업

### PR-X (🔴 보안 critical)
- e2e/helpers/admin-credentials.ts 신규 (E2E_ADMIN_PW throw + fallback ADMIN_EMAIL)
- 6 prod-targeting spec import 통일 (`123456` 평문 + env fallback 제거)
- crm-8skill (localhost+test 비번) 별도 유지

### PR-A (Q-A1)
- /dashboard/farms-board §이력 검색 행 클릭 silent fail 해결
- expired/cancelled 임대: toast.info 안내 ("만료된 임대는 상세 보기를 제공하지 않습니다")
- active 임대: 기존 FarmDrawer 오픈 (무변경)

### PR-C1 (만료 KPI 한 카드 2-값)
- `lib/constants.ts` EXPIRY_WARNING_DAYS(30) / EXPIRY_DANGER_DAYS(7) 재사용
- `lib/use-data.ts`: `expiringThisMonth` → `farmExpiringIn7 + farmExpiringIn30` (컨텍스트 명시)
- `StatsCards.tsx` "이달 만료" → "임대 만료 임박" (D-7 빨강 + D-30 주황)
- `FarmsBoardKpi.tsx` "만료 임박 (30일)" → "만료 임박" (D-7 + D-30)
- `MembershipStatsCards.tsx` "만료 임박 (D-30)" → "회원권 만료 임박" (D-7 + D-30)
- `MembershipsPageClient.tsx` stats 분리 (expiringIn7 + expiringIn30)
- `e2e/production-membership.spec.ts:60` 라벨 정규식 사전 패치

### PR-C2 (ConfirmDialog)
- `components/ui/ConfirmDialog.tsx` 신규 (3 variant + z-60 + role="alertdialog")
- `components/ui/useConfirm.tsx` 신규 (async/await 패턴 hook)
- Phase 1 4건 마이그:
  - plans/page.tsx (플랜 삭제) destructive
  - notices/page.tsx (공지 삭제) destructive
  - AckControls.tsx (일괄 ack) default
  - FacilitiesTable.tsx (시설 삭제) destructive

### PR-B (Playwright 1.60)
- `@playwright/test` 1.59.1 → 1.60.0
- 11 spec / 82 tests 모두 1.60 컴파일 통과 (`--list` 검증)
- 운영 코드 영향 0

---

## 🟡 잔여 작업 (다음 세션 / kk 액션)

### 🔴 kk 즉시 액션 (이번 주)
1. **Prod 어드민 비번 rotation** (Supabase Dashboard)
2. **GitHub Secrets**: E2E_ADMIN_PW 등록 + CI workflows env 매핑
3. **local .env.local**: E2E_ADMIN_PW 추가
4. **Cloudflare wildcard cert 정리** (Dashboard)

자세한 안내: `thoughts/sessions/20260517-0100_kk_action_required.md`

### 🟡 5/22 이후 kk 액션
5. **U2 모바일 햄버거 활성화** (`NEXT_PUBLIC_SIDEBAR_MOBILE_V2=1`)

### 🟢 5일 lead time
6. **Aligo 알림톡 템플릿 등록 + 카카오 검수** (PR-D 사전 작업)

### 🔵 다음 세션 (Claude)
7. **PR-D 알림톡 D-30 cron** (Aligo 검수 후, 마이그 088 + 3단계 패턴 + slackAlert + timezone + 글로벌 게이트)
8. **ConfirmDialog Phase 2-3** (9 + 9 = 18건 점진적 마이그)
9. **Realtime 401 옵션 (C)** Supabase Dashboard / (D) support

---

## 📊 prod 데이터 (01:00)

```
회원              60+ approved
농장              60 / rentals 80 (active 34 / expired 33 / cancelled 11)
임차인 (customers) 144
BBQ 예약          30+ / 평상 1상품 / 이벤트 1
공지              7
인덱스 trgm       customers + members 모두 활성 (082+087)
realtime pub      notifications + member_notifications + bbq_reservations + notices
RPC               get_farms_board (085), search_farm_rentals_history (087)
KPI 분리          D-7 + D-30 한 카드 2-값 (Stats/FarmsBoard/Membership 3 페이지)
ConfirmDialog     Phase 1 4건 마이그 완료 (z-60, 3 variant)
```

---

## 🛡 이번 세션 신규 보호 장치

| 종류 | 항목 |
|---|---|
| 보안 | e2e/*.spec.ts ADMIN_PW 환경변수 강제 (throw on missing) |
| UX | expired/cancelled 임대 클릭 → toast 안내 (silent fail 회피) |
| KPI | 만료 임박 D-7 (빨강) + D-30 (주황) 시각 위계 분리 |
| 일관성 | EXPIRY_DANGER/WARNING_DAYS 상수 재사용 (4 명명 중복 회피) |
| a11y | ConfirmDialog role="alertdialog" + focus trap + ESC |
| z-index | ConfirmDialog z-60 (Modal/Drawer z-50 위) |
| 테스트 | Playwright 1.60 (호환성 검증 + 11 spec 정상) |

---

## ⚠️ Realtime 401 (변동 없음)

- 6차 시도 (옵션 7) 후 5건 잔존 (82% 감소)
- WebSocket upgrade 자체 401 (sent/received frames 0)
- client-side 한계 도달
- 다음 세션: 옵션 (D) Supabase support / GitHub issue (외부 0 risk)

---

## 🔴 운영 정합성 체크 SQL (다음 세션 진입 전)

```sql
SELECT
  (SELECT COUNT(*) FROM bbq_reservations) AS bbq_total,
  (SELECT COUNT(*) FROM farms WHERE deleted_at IS NULL) AS farms_total,
  (SELECT COUNT(*) FROM farm_rentals) AS rentals_total,
  (SELECT COUNT(*) FROM farm_rentals WHERE status='active') AS rentals_active,
  (SELECT COUNT(*) FROM memberships WHERE status='active') AS memberships_active,
  (SELECT COUNT(*) FROM memberships WHERE status='active' AND end_date <= CURRENT_DATE + INTERVAL '7 days') AS expiring_7d,
  (SELECT COUNT(*) FROM memberships WHERE status='active' AND end_date <= CURRENT_DATE + INTERVAL '30 days') AS expiring_30d,
  (SELECT COUNT(*) FROM audit_logs WHERE action='farm_history_search' AND created_at > NOW() - INTERVAL '24 hours') AS farm_hist_dedup,
  (SELECT COUNT(*) FROM audit_logs WHERE action='farms_board_view' AND created_at > NOW() - INTERVAL '24 hours') AS farms_board_dedup,
  (SELECT COUNT(*) FROM audit_logs WHERE action LIKE '%_unauthorized') AS unauthorized_total;
```

---

## 🧠 컨텍스트 복구 파일

1. 이 인계: `thoughts/sessions/20260517-0100_handover.md`
2. kk 액션: `thoughts/sessions/20260517-0100_kk_action_required.md`
3. v2 plan: `thoughts/plans/20260516-2500_residual_backlog_v2_critical_fix.md`
4. 이전 인계: `thoughts/sessions/20260516-2330_handover.md`

---

## 핵심 상수 / 링크

- admin: `admin@pocolush.co.kr` / **rotation 필요**
- Supabase project: `lhuaxmzsvrmjavanunnv`
- prod URL: `https://app.pocolush.com` / `https://www.pocolush.com`
- 다음 마이그 번호: **088** (PR-D 알림톡 cron 인프라용)
- Playwright: 1.60.0 (업그레이드 완료)

---

## END — 8스킬+7점+사이드 이펙트 검수 후 5 PR 안전 배포. PR-D 는 Aligo 검수 후 다음 세션.
